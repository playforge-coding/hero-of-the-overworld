/target
/dist
# Cargo.lock is committed: this repo produces a binary/app, so CI should build
# against a pinned, reproducible dependency set.
**/*.rs.bk
debug/

# Built docs site (Zensical output).
/site

# Self-hosted webfonts, downloaded from Google Fonts at build time by
# scripts/fetch-fonts.py (run before `zensical build`).
/docs/fonts/
/docs/stylesheets/fonts.css
